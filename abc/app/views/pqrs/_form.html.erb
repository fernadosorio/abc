<%= form_with(model: pqr, local: true) do |form| %>
  <% if pqr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pqr.errors.count, "error") %> prohibited this pqr from being saved:</h2>

      <ul>
      <% pqr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
    <% unless user_signed_in? %>s
  <div class="field col s12 m6">
        <%= form.label :title %>
        <%= form.text_field :title %>
    </div>

    <div class="field col s12 m6">
        <%= form.label :subject %>
        <%= form.text_field :subject %>
    </div>

    <div class="field col s12 m6">
        <%= form.label :body %>
        <%= form.text_area :body %>
    </div>

    <div class="field col s12 m6 hide">
        <%= form.label :creation_date %>
        <%= form.datetime_select :creation_date %>
    </div>

    <div class="field col s12 m6">
        <%= form.label :expiration_date %>
        <%= form.datetime_select :expiration_date %>
    </div>

    <div class="field col s12 m6">
            <%= form.label :user_id %>
            <%= form.text_field :user_id %>
    </div>
  <% end %>
   
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
